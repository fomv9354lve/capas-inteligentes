# MOAT_EDIT_QUEUE — surgical human-apply edit list

Each row below is one curated-site/docs claim that failed refutation under REGLA CERO
(every claim BACKED only if re-derivable from a NAMED repo artifact/command). A human
applies these by hand. **Do not auto-edit the live files** — the site is curated.

Verdict legend: **DELETE** = strike, unbacked & unscopeable · **REWRITE** = re-word to
what artifacts back · **SCOPE** = true but must be bounded to a named set/device · **BACKED**
= survives as-is, no edit.

Ordering = impact. No claim in this batch scopes to DELETE; the top block is the
unbacked **comparative / universal-negative** REWRITEs (strip an absence-claim or
adoption-as-fact — highest risk), then the **aspirational-as-fact moat** REWRITEs, then
the bounded **SCOPE** fixes, then **BACKED** (no action).

Counts: **0 DELETE · 14 REWRITE · 1 SCOPE · 0 BACKED** (15 verdicts total).

---

## BLOCK A — Unbacked comparative / universal-negative (apply first)

### A1 · `index-html-360-gap-claim` — REWRITE (universal negative)
- **Where:** `docs/index.html:360` — "Why CAPAS" / "Not an LLM checker. Not a plagiarism tool.", `.section-sub`
- **CURRENT:** "CAPAS fills a gap that existing tools leave open: structured, deterministic admissibility gating for scientific claims."
- **HIT:** Universal-negative ("fills a gap that existing tools leave open" = absence over ALL tools — never BACKABLE) + aspirational-as-fact (backing audit files it as HOLD / unvalidated, `GLOBAL_SOTA_MARKET_AUDIT.md:321-323,336`) + copyable-rung (the broad "gap" claims territory `sota_positioning_matrix.json` `claims_to_avoid` forbids). Only "structured, deterministic admissibility gating" is BACKED (`CLAIMS_REGISTRY.md:14`, CLOSED).
- **-> NEW:** Keep the h2 "Not an LLM checker. Not a plagiarism tool." then replace the `.section-sub` with:
  > Not an LLM checker. Not a plagiarism tool. Across the systems we surveyed — RO-Crate / Workflow Run RO-Crate, SciAgentGym, QMB100/PhysVEC, AiiDA, OpenKIM/VVUQ, C2PA, and ClaimCheck-class fact-checkers (see GLOBAL_SOTA_MARKET_AUDIT) — none found made physical-evidence strength and witness independence first-class enough to weaken, reject, or hold a claim rather than scoring it pass/fail. CAPAS does, deterministically: same input always yields the same audit_hash, with no language model in the verdict. You don't have to take our word for it — run the conformance suite yourself (`python3 benchmarks/conformance.py`) and you get the same CAPAS-CONFORMANT verdict and result hash, plus a signed certificate. That self-checkable mark, not the individual checks, is the moat.
  - **Note to applier:** preserve the named surveyed set + the audit reference VERBATIM so the negative stays survey-scoped, not universal.
- **Re-derive:** `docs/CLAIMS_REGISTRY.md:14` (CLOSED) via `benchmarks/verify_cara_decoupling.py` (+ audit_hash determinism); `docs/GLOBAL_SOTA_MARKET_AUDIT.md:18-20,30-47,321-323,336` + `docs/sota_positioning_matrix.json` (defensible_claim / claims_to_avoid / falsation_condition); `benchmarks/conformance.py` + `capas_certstore.py`.

### A2 · `index-html-384-positioning` — REWRITE (universal negative)
- **Where:** `docs/index.html:384` — `<p class="section-sub">` under "The other half of the stack"
- **CURRENT:** "Frontier models and agents generate claims. None of them gate whether a claim is licensed by its evidence before reuse. That side of the stack is empty — and it is the side that governs. CAPAS is evidence-governance infrastructure that starts at that gate."
- **HIT:** Universal-negative-as-fact ("None of them gate" / "that side of the stack is empty" over every system, no surveyed set). The repo's own strongest validation artifact refutes "empty": `docs/capas_vs_sota_and_ibm.md` shows IBM's calibration system IS structurally a production admissibility engine. The page's own vs-table at `:390-391` lists LLM-judge / fact-checkers as partially gating — internal contradiction.
- **-> NEW:** Replace the `<p class="section-sub">` text with:
  > Frontier models and agents are built to generate claims. The systems adjacent to them — LLM-as-judge, fact-checkers, domain validators like Pinnacle&nbsp;21 / statcheck, even IBM's production calibration engine (the closest structural match we've found; see capas_vs_sota_and_ibm.md) — each gate only a slice: they assess truth, or run stochastically and non-replayably, or gate one domain's physical-qubit claims, not arbitrary evidence-licensed ones. Across the systems we surveyed we found no general-purpose, deterministic, replayable gate that decides whether a claim is licensed by its evidence before reuse — and proves it the same way twice. CAPAS starts there: a fail-closed admissibility gate whose verdict is re-derivable to a signed certificate and a conformance hash anyone can reproduce.
  - **Note:** h2 at `:383` ("should this have been said?") needs no change. Ensure the IBM/LLM-judge vs-table rows are not re-contradicted by this lede.
- **Re-derive:** `benchmarks/verify_fail_closed.py` + `benchmarks/verify_cara_decoupling.py` (both CLOSED, `CLAIMS_REGISTRY.md`); `benchmarks/conformance.py` -> `outputs/conformance_result.json` (conformant:true, result_hash sha256:3d3b5cf39aaffef23fa8562b); `capas_certstore.py` issue()/verify(); `docs/capas_vs_sota_and_ibm.md`; `head_to_head_sota.py` SCOPED (10-claim corpus). Deleted "None of them" / "is empty" — no artifact backs it.

### A3 · `moat-vs-table-other-half` — REWRITE (universal negative + copyable rung)
- **Where:** `docs/index.html:383-394` — "The other half of the stack" sub-paragraph + `table.vs-table`
- **CURRENT:** sub `:384` "None of them gate... That side of the stack is empty"; vs-table `:385-394` (GPT/Claude/Gemini, deep-research agents, LLM-as-judge "stochastic, no guarantee, not replayable", fact-checkers "truth, not boundary", CAPAS "deterministic, replayable, fail-closed (test-proven)").
- **HIT:** Universal-negative at `:384`; the surveyed set is the NAMED modeled set in `head_to_head_sota.py` only. Copyable-rung: the table is a generate-vs-gate positioning frame, never references the locked conformance suite + self-run mark (the actual non-copyable moat). LLM-judge "not replayable" asserts a measured test that does not exist (single run `h2h_llm_judge_run.json`).
- **-> NEW:** Replace the `.section-sub` at `:384` AND the full `table.vs-table` `:385-394` with:
  ```html
  <p class="section-sub">Frontier models and agents generate claims; CAPAS gates whether a claim is licensed by its evidence before reuse. Across the SOTA approaches we put on one labeled corpus — LLM-as-judge, optimistic-oracle (UMA), peer-prediction (BTS) and reputation (EigenTrust) (benchmarks/head_to_head_sota.py) — none decide evidence-licensing deterministically and replayably. The CAPAS row below is the one not copyable by re-running a table: it is a self-run conformance mark — anyone clones the repo and re-derives the same 8-invariant result_hash and per-verdict audit_hash bit-for-bit (benchmarks/attest_conformance.py, benchmarks/conformance.py; GOVERNANCE.md sections 5 and 6).</p>
  <table class="vs-table">
    <thead><tr><th>System</th><th>Generates claims</th><th>Gates claims</th></tr></thead>
    <tbody>
      <tr><td>GPT &middot; Claude &middot; Gemini</td><td class="yes">&#10003;</td><td class="no">&#10007;</td></tr>
      <tr><td>Deep-research agents</td><td class="yes">&#10003;</td><td class="no">&#10007;</td></tr>
      <tr><td>LLM-as-judge</td><td class="no">&#10007;</td><td class="part">stochastic by construction &middot; no replay guarantee</td></tr>
      <tr><td>Fact-checkers</td><td class="no">&#10007;</td><td class="part">truth, not boundary</td></tr>
      <tr class="capas"><td>CAPAS</td><td class="no">&#10007;</td><td class="yes">&#10003; deterministic &middot; replayable &middot; fail-closed (conformance-test-proven)</td></tr>
    </tbody>
  </table>
  ```
  - Net: (1) strike "None of them gate... That side of the stack is empty", bound to the named set with `benchmarks/head_to_head_sota.py` inline; (2) re-anchor the CAPAS row on the self-run conformance mark (`attest_conformance.py`, `conformance.py`, GOVERNANCE §5/6); (3) soften LLM-judge "not replayable" -> "no replay guarantee" + "stochastic by construction"; (4) "(test-proven)" -> "(conformance-test-proven)".
- **Re-derive:** `benchmarks/head_to_head_sota.py` (4 named arms — bounds the negative); `benchmarks/verify_fail_closed.py` + `outputs/conformance_result.json` (fail_closed pass); `benchmarks/conformance.py` (8-item suite + result_hash); `benchmarks/verify_audit_hash_reproduces.py`; `benchmarks/attest_conformance.py` (GOVERNANCE §5/6); `benchmarks/h2h_llm_judge_run.json` (determinism NO/stochastic).

### A4 · `moat-quantum-oracle-no-competitor` — REWRITE (unbacked comparative negative)
- **Where:** `docs/MOTOR_INGEST_LOG.md:25` — SIGNAL 2026-06-24 ingest table, row #7 (kind: moat)
- **CURRENT:** "Moat: as a device-calibrated quantum-vs-classical oracle, no published competitor [survey-scoped REWRITE: no published competitor in the surveyed SOTA set (Stim, quimb, cotengra, Cirq-qsim, Pan-Zhang, Tindall, AGLLV, Begušić-Chan, IBM QProv, QuAntiL) does device-calibrated quantum-vs-classical routing + frontier prediction]."
- **HIT:** (1) Unbacked comparative negative — the ten-tool set appears in exactly ONE file (`grep -rln "QuAntiL|QProv"` -> `docs/MOTOR_INGEST_LOG.md` only); no capability matrix characterizes any competitor; row #6 self-concedes "Not independently verified by CAPAS". (2) Aspirational-as-fact — "device-calibrated frontier prediction" is "same-device validated, cross-device pending" (`CLAIMS_REGISTRY.md`). (3) Copyable rung — the routing half (`verify_route.py`) is a structure switch.
- **-> NEW:** Replace the row's "Licensed wording:" sentence in the Evidence/why cell with:
  > **Moat (positioning hypothesis, not a measured moat).** On the motor's *cited, not independently re-verified* reading of the surveyed SOTA set — Stim, quimb, cotengra, Cirq-qsim, Pan-Zhang, Tindall, AGLLV, Begušić-Chan, IBM QProv, QuAntiL — none is *documented* to combine device-calibrated quantum-vs-classical routing with simulability-frontier prediction. Two debts remain un-discharged: (a) no per-competitor capability matrix exists in-repo (the named set occurs only in this log; upgrade per row #6 = attach a cited capability matrix), and (b) the device-calibrated `commitment_depth` frontier is **same-device validated, cross-device pending** (`docs/CLAIMS_REGISTRY.md`). What IS closed and re-derivable today: the routing layer (`benchmarks/verify_route.py`) and the gate contract (`benchmarks/verify_quantum_commitment.py`) — but routing is a structure-based dispatcher (copyable), so it is not itself the moat. The defensible moat is NOT "no competitor does X"; it is the **self-run conformance mark plus the signed, audit-hashed certificate** CAPAS issues on each gated claim (`benchmarks/conformance.py`, `capas_certstore.py`): a competitor can copy the routing switch in a week but cannot retroactively issue CAPAS's certification trail. Until (a) and (b) are discharged, treat the comparative-negative line as a positioning hypothesis, not a benchmark.
  - Change the verdict tag from "REWRITE · FLAG / Survey-scoped, that is BACKED" to "REWRITE · FLAG -> positioning hypothesis (two debts: no competitor matrix; frontier same-device-only); moat re-anchored on conformance mark + signed certificate". Update the row-#7 line in the "Net:" and "Sent back to the motor:" summaries so it no longer asserts the survey-scoped form "is BACKED".
- **Re-derive:** `grep -rln "QuAntiL|QProv" .` -> single hit (proves the set is not re-derivable from a 2nd artifact); `docs/CLAIMS_REGISTRY.md` quantum_commitment row (same-device validated, cross-device pending); `benchmarks/verify_quantum_commitment.py` (contract only); `benchmarks/verify_route.py` (copyable dispatcher); `benchmarks/conformance.py` + `capas_certstore.py` (actual moat); `docs/MOTOR_INGEST_LOG.md` row #6 (names the required upgrade).

---

## BLOCK B — Aspirational-as-fact "the standard / the moat" REWRITEs

### B1 · `moat-standard-claim` — REWRITE (adoption-as-fact + phantom ledger)
- **Where:** `docs/index.html:603-604` — "Why it compounds": h2 `:603`, p `:604`
- **CURRENT (h2 :603 / p :604):** "The moat isn't any single gate. It's becoming the standard claims are checked against. ... the reference that keeps surviving refutation becomes the standard. CAPAS is built to be that reference: ... a survive-refutation ledger anyone can challenge. The standard, not the tool, is the moat."
- **HIT:** "becoming the standard" / "becomes the standard" = present-progressive traction with NO external-adoption artifact (`reviewer_registry.json` = one `local_registry_fixture`; `witness_registry.json` = one `analytic_no_solver` anchor; CLAIMS_REGISTRY / proof_ledger = CAPAS gating its OWN claims). Implicit universal-negative ("THE standard" presumes no incumbent — but `:621` concedes Pinnacle 21). Phantom artifact: "survive-refutation ledger anyone can challenge" — `demo_survive_refutation.py` writes only an ephemeral `/tmp/capas_ledger_demo/ledger.json` wiped each run.
- **-> NEW (h2 :603):** "The moat isn't any single gate. It's the audit surface a standard is made of: self-runnable, deterministic, irrevocably governed."
- **-> NEW (p :604):**
  > Any one gate is copyable. What compounds is trust you can audit. Conformance is self-runnable and deterministic: `python3 benchmarks/conformance.py` runs the exact suite the certifier runs and returns the same verdict and the same result hash on any machine. Each issued certificate is signed, persisted, and content-addressed (`capas_certstore.py`), so a regulated buyer can reproduce the verdict and a third party can try to refute it. And the mark that attests it is pre-committed to neutral governance in writing, irrevocably, before adoption (`GOVERNANCE.md` §3) — the move that let Open Policy Agent's standard outlast its sponsor's acquisition (§85). A competitor can copy a gate in a week; it cannot retroactively copy a self-runnable conformance mark, a signed-certificate audit trail, and a renounced-in-writing relicensing posture. That is the moat: not the tool, the auditable standard around it.
- **-> NEW (append trailing p):** as a separate `p.section-sub style="margin-top:14px;opacity:.7"` (mirror IBM-section pattern at `:585`):
  > Honest scope: this is the design and the audit surface — not an adoption claim; no external party has yet certified against the mark.
  - **Applier notes:** escape entities to file style (don't -> don&rsquo;t, em-dashes -> &mdash;), wrap `python3 benchmarks/conformance.py` / `capas_certstore.py` / `GOVERNANCE.md §3` in `<code>`; keep the existing inline style attrs on the h2. DROP "becoming the standard" / "keeps surviving refutation becomes the standard" / "a survive-refutation ledger anyone can challenge".
- **Re-derive:** `benchmarks/conformance.py` (self-run -> CAPAS-CONFORMANT + reproducible result_hash); `capas_certstore.py` (signed, content-addressed cert, id = sha256(canonical+nonce)); `GOVERNANCE.md` §3 (irrevocable ratchet) + §85 (Styra/OPA model); `docs/CLAIMS_REGISTRY.md` (audit_hash determinism via `benchmarks/verify_audit_hash_reproduces.py`). Universal-negative dropped per `index.html:621` (Pinnacle 21 incumbent).

### B2 · `index-604-moat` — REWRITE (compound; aspirational + universal-negative half)
- **Where:** `docs/index.html:604` — "Why it compounds", h2 `:603` + section-sub `:604`
- **CURRENT:** "Any one gate is copyable. What compounds is trust you can audit: a deterministic, re-derivable, fail-closed verdict a regulated buyer can reproduce and a third party can try to refute. ... the reference that keeps surviving refutation becomes the standard. CAPAS is built to be that reference ... The standard, not the tool, is the moat."
- **HIT:** Three sub-clauses BACKED and survive (audit_hash re-derivation, the CLOSED/BACKED/SCOPED ledger, the survive-refutation mechanism). REFUTATION HIT = present-tense "the reference that keeps surviving refutation BECOMES the standard ... is the moat": (a) "becomes the standard" = external-adoption claim no artifact re-derives (1 `local_registry_fixture` + 1 `analytic_no_solver`; zero external party); (b) "THE standard" with no incumbent named = latent universal-negative (Pinnacle 21 at `:621`).
- **-> NEW (section-sub :604):**
  > Any one gate is copyable. What is hard to copy is trust you can audit &mdash; and CAPAS is built so trust is <strong>earnable, not asserted</strong>. Every verdict re-derives to a hash an independent party can reproduce and tampering diverges (<code style="color:rgba(255,255,255,.7)">benchmarks/verify_audit_hash_reproduces.py</code>); every headline claim is <strong>CLOSED / BACKED / SCOPED</strong> in a public ledger that discloses which numbers are synthetic (<code style="color:rgba(255,255,255,.7)">docs/CLAIMS_REGISTRY.md</code>); the certification mark attests only that an artifact passed a suite <em>you run yourself</em> and get the same verdict and the same hash (<code style="color:rgba(255,255,255,.7)">python3 benchmarks/conformance.py</code>), issued as a signed, content-addressed certificate (<code style="color:rgba(255,255,255,.7)">capas_certstore.py</code>). Standing in the attestation ledger is earned only by surviving refutation, never declared (<code style="color:rgba(255,255,255,.7)">benchmarks/demo_survive_refutation.py</code>). The moat we claim is the self-runnable conformance mark and signed certificate &mdash; mechanism a regulated buyer can reproduce without trusting us. Whether it <em>becomes</em> the reference standard depends on third parties actually challenging it; that adjudication is open, not yet run.
- **-> NEW (h2 :603):** soften present-tense outcome to mechanism, e.g.: "The moat isn't any single gate. It's a conformance mark and signed certificate you can re-run, not trust."
- **-> Also:** cut every remaining present-tense "is the standard / is that reference / the standard is the moat" wording in this section.
  - **Note:** B1 and B2 both target `:603-604`. Apply as one coherent rewrite of the "Why it compounds" section (lines 600-606); B1's h2 + B2's section-sub body are mutually compatible — use B1's h2 phrasing and B2's section-sub body, plus B1's trailing honest-scope `<p>`.
- **Re-derive:** `benchmarks/verify_audit_hash_reproduces.py` (+ verify_fail_closed.py, verify_cara_decoupling.py, CLOSED per CLAIMS_REGISTRY); `docs/CLAIMS_REGISTRY.md` (regenerated by `benchmarks/generate_claims_registry.py`); `benchmarks/conformance.py`; `capas_certstore.py`; `benchmarks/demo_survive_refutation.py`. Adoption scoped "open, not yet run" via `reviewer_registry.json` (1 fixture) + `witness_registry.json` (1 anchor). Universal-negative removed (Pinnacle 21, `:621`).

### B3 · `index.html:596:moat` — REWRITE (inverted moat / aspirational asset)
- **Where:** `docs/index.html:596` — closing "re-derive it yourself" line of "Independent validation" (kind: moat)
- **CURRENT:** "Open engine: the method is inspectable; the standard is the asset."
- **HIT:** (A) "the method is inspectable" BACKED but is the FULLY COPYABLE half (Apache-2.0; `:604` concedes "Any one gate is copyable"). (B) "the standard is the asset" NOT BACKED present-tense: `GOVERNANCE.md:22-23` trustee = "[TRUSTEE — to be named]"; `:100-101` escrow "operative once" trustee named; `:106` open items "(1) name the trustee; (2) execute the escrow". No adoption artifact in-repo. Inverts which half is the moat.
- **-> NEW (replace the final sentence of the :596 paragraph):**
  > Open engine, Apache-2.0 (LICENSE): the method is fully inspectable and the verdict re-derivable by anyone (benchmarks/conformance.py runs the exact certifier suite and returns the same verdict and the same hash; capas_certstore.py issues a signed, tamper-evident certificate you can verify). The defensible asset isn't the copyable engine — it's the self-runnable conformance mark, pre-committed to neutral trustee-escrow in a binding, irrevocable charter (GOVERNANCE.md), with the trustee and escrow named as open items before it is operative.
- **Re-derive:** `benchmarks/conformance.py` (line 8 invocation -> CONFORMANT + result hash); `capas_certstore.py:45-77` (sign/issue/verify); `LICENSE` + `NOTICE` (Apache-2.0 + reserved mark); `GOVERNANCE.md §1`, §2 + lines 22-23/100-101/106 (mark reserved, trustee-escrow vehicle, open items disclosed).

### B4 · `moat-consilience-583` — REWRITE (unbacked superlative + reach overstatement)
- **Where:** `docs/index.html:583-585` — "Independent validation"
- **CURRENT (h2 :583 / body :584 / scope :585):** "We didn't invent the architecture. The most demanding hardware stack in the world already runs it. ... CAPAS generalizes the same mechanism across ten domains — finance, statistics, epidemiology, chemistry, physics, quantum. Two independent systems converging on the same admissibility mechanism is consilience: evidence the design is structural, not a pitch. [Honest scope: identities textbook, convergence architectural not a partnership claim; on frozen calibration CAPAS is the stricter of the two]"
- **HIT:** (1) Unbacked superlative "the most demanding hardware stack in the world" (ranked by no artifact). (2) Reach overstatement — "the same mechanism across ten domains" conflates 26 gates / 10 domains (`proof_ledger.json:40`) with the single invariant filter demonstrated on only finance/psychology/quantum (`capas_vs_sota_and_ibm.md:65`); list names only 6. (3) Copyable-rung conflation — consilience VALIDATES a copyable (Apache-2.0) mechanism; it is not the moat.
- **-> NEW (h2 :583):** "We didn't invent the architecture — a frontier quantum-hardware vendor's production stack already runs it."
- **-> NEW (body :584):**
  > IBM's quantum stack will not run your circuit until it clears a calibration gate — every job checked against frozen, re-derived device invariants, fail-closed, with no model-of-the-day in the decision. That is exactly the CAPAS architecture: re-derive from declared evidence, refuse on violation, keep the verdict deterministic. It already runs in production, at the frontier of physics. CAPAS runs 26 such gates across 10 domains (proof_ledger.json); the single fail-closed invariant filter is demonstrated end-to-end across finance, psychology, and quantum. Two independent systems converging on the same admissibility mechanism is consilience: evidence the design is sound, not a pitch.
- **-> NEW (honest-scope :585):**
  > Honest scope: the identities CAPAS checks are textbook and the convergence is architectural, not a partnership claim. The mechanism itself is the open Apache-2.0 engine — re-derivable, therefore copyable — so the consilience validates the design but is not the moat. What is defensible is the cross-domain composition plus the self-run conformance mark and signed certificate (benchmarks/conformance.py, capas_certstore.py): a deterministic verdict re-derivable to a hash that a third party reproduces and tries to refute. On frozen calibration, CAPAS is in fact the stricter of the two.
- **Re-derive:** `examples/kingston_live_audit.py` (IBM fail-closed gate, 155 qubits/176 edges, Q121, frozen-calibration strictness); `docs/capas_vs_sota_and_ibm.md:11-23,65`; `docs/proof_ledger.json:40` + `docs/capability_matrix.md` (26 gates/10 domains); `benchmarks/conformance.py` + `capas_certstore.py`; `benchmarks/kingston_real_bell_verdict.json` (consilience; coarse-grained per `first_order.note` / `calibrated.resolution_note`).

---

## BLOCK C — Governance / certstore / registry aspirational-as-fact REWRITEs

### C1 · `GOV-mark-reserved-irrevocable` — REWRITE (unbacked citation + aspirational legal effect)
- **Where:** `GOVERNANCE.md:9-35` — §1 open core, §2 reserved mark + "effective now" escrow, §3 irrevocability
- **CURRENT:** "The CAPAS name and the official certification/conformance mark are reserved (see NOTICE) ... pre-commits them to neutral governance before CAPAS is promoted for adoption ... The commitments in §1,§2,§4,§5 are irrevocable and may be amended only in the direction of greater neutrality and openness, never less."
- **HIT:** (1) Unbacked citation — §2 cites `NOTICE` as proof the mark "is reserved"; NOTICE (11 lines) is a bare Apache-2.0 notice (grep reserv/trademark/mark/certification = ZERO); README points back to same empty NOTICE (circular); no TRADEMARKS.md / no filing. (2) Aspirational-as-fact — escrow "effective now" but status block `:100-107` admits trustee = "[TRUSTEE — to be named]" (`:23,101`), vehicle "operative once... escrow is executed", open items "(1) name the trustee; (2) execute the escrow". (3) §3 "irrevocable... void" is legal effect the doc doesn't yet have (`:102` concedes terms "to be finalized in an executed legal instrument"). (4) Copyable rung by its own admission (§8 "the model this charter copies" = CNCF/Styra-OPA). Soft universal-negative §3/§7 (5-row table generalized to causal law).
- **-> NEW (§1, lines 9-14):**
  > The CAPAS engine, schema, calculus, reference gate, CLI, tests, and benchmark corpus are open under Apache-2.0 (see LICENSE, THIRD_PARTY_NOTICES.md). This charter is the maintainer's public, binding statement of intent not to relicense them to a non-OSI license; that intent is to be made legally irrevocable by an executed instrument (see status block).
- **-> NEW (§2, lines 16-26):**
  > The CAPAS name and the official certification/conformance mark are claimed by the maintainer (Copyright 2026 Fco. Osvaldo Morales Vilchis, see NOTICE and LICENSE §6) and are intended to be reserved and placed under neutral governance before CAPAS is promoted for adoption. No trademark registration has been filed and no escrow has been executed yet — both are open items in the status block. Intended vehicle: trustee-escrow on the Certified-Kubernetes pattern (trustee not yet named); destination: assignment to an independent foundation. What is operative today is not the legal reservation but the self-run conformance mark: the mark attests only that an artifact passed the open, deterministic suite (§5), which anyone can re-derive — see python3 benchmarks/conformance.py and POST /api/gate/verify (capas.verify_audit_hash). That self-runnable check, not a registration, is the asset this charter protects.
- **-> NEW (§3, lines 28-35):**
  > The commitments in §1, §2, §4, and §5 are drafted to be made irrevocable — amendable only toward greater neutrality and openness — by an executed legal instrument. Until that instrument is executed by counsel (status block, open item 2) they are a public, non-binding statement of direction, not yet a legally enforceable one-way ratchet. The capture move this charter forbids is the configuration that, in the surveyed set in §7, preceded a neutral-foundation fork in every listed case.
- **-> NEW (§7 intro, lines 76-77):**
  > In the surveyed set below, every project that relicensed its open core to a non-OSI license triggered a neutral-foundation fork that stranded the original; reversals did not restore trust. [table unchanged]
  - **Recommended companion fix:** either add affirmative reservation text to `NOTICE` / create `TRADEMARKS.md`, OR remove the "(see NOTICE)" citation from §2 and `README.md:39,107` so nothing cites the empty NOTICE as reservation backing.
- **Re-derive:** `benchmarks/conformance.py` (exists); `POST /api/gate/verify` -> `capas.verify_audit_hash` (`capas_api.py:100-102`); `capas_certstore.py`. NOTICE backs only authorship/copyright + Apache-2.0, NOT mark reservation (11 lines, grep empty). `GOVERNANCE.md` status block `:100-107` self-refutes "reserved/irrevocable/escrow effective now" as open items. §7 = named 5-row survey, not a law.

### C2 · `moat-conformance-selfrun-deterministic` — REWRITE (copyable rung + aspirational pipeline + over-claim)
- **Where:** `GOVERNANCE.md:51-58` — §5 "Self-run, deterministic conformance"; echoed `benchmarks/conformance.py:1-13,67`
- **CURRENT:** "Conformance is self-runnable and deterministic. Anyone may run the same open conformance suite the certifier runs (python3 benchmarks/conformance.py) and obtain the same verdict and the same audit hash ... The mark attests that a verified artifact passed that suite; it never requires trusting a private process."
- **HIT:** (1) Copyable rung — self-admitted "modeled on Certified Kubernetes / Sonobuoy" (`GOVERNANCE.md:56`, `conformance.py:1-4`); the non-copyable part is the specific 8 invariants + mark + signed cert. (2) Aspirational — "the certifier runs" / "yearly re-certification" describe a process with no shipping artifact (no certifier, no PR pipeline, no annual cycle; `/api/gate/verify` checks a hash, doesn't issue/revoke). (3) Over-claim — `:67` "reproduces on any machine" but hash at `:50-51` covers only the [(id,pass)] boolean set, no pinned interpreter/lockfile; proven = re-run determinism, NOT cross-machine.
- **-> NEW (replace §5, lines 51-58):**
  > ## 5. Self-run conformance over a fixed invariant set
  >
  > The conformance suite is open and self-runnable: anyone may run `python3 benchmarks/conformance.py` and, given an identical suite-pass profile, re-derive the same verdict and the same conformance hash — verified here as `sha256:3d3b5cf39aaffef23fa8562b`, byte-identical on re-run against the committed `outputs/conformance_result.json`. The result hash is taken over the load-bearing pass/fail set (fail-closed, robustness, no-LLM-in-verdict, quantum-defeater, pharma-admissibility, proof-ledger, hold-has-resolution, audit-hash-reproduces); it is reproducible on re-run, not guaranteed across arbitrary environments (no pinned interpreter or lockfile is asserted). Separately, any individual gate result can be re-derived and checked for tampering via `POST /api/gate/verify` (`capas.verify_audit_hash`).
  >
  > The self-run, deterministic harness is a known distribution mechanic, modeled on Certified Kubernetes / Sonobuoy, and is not itself the moat — it is reproducible by any party over their own checks. What is not freely copyable is (a) **this specific load-bearing invariant set** as the definition of "CAPAS-conformant", (b) **the mark** gating that definition, and (c) **the signed, persisted, re-derivable admissibility certificate** the engine issues (`capas_certstore.py` — content-bound HMAC signature over the certificate body). The mark attests that a verified artifact passed exactly this suite; checking it never requires trusting a private process.
  >
  > > Governance design (not yet a running pipeline): the intended model is a certifier running this same open suite, PR-submitted conformance, and yearly re-certification to prevent stale forks. As of this writing the repo ships the open suite and the verify route; the mark-granting, PR-submission, and annual re-certification process is governance design, not an operating service. This conformance right is irrevocable under §3.
- **-> Also align** `benchmarks/conformance.py:67`: change "(deterministic — reproduces on any machine)" to "(deterministic on re-run — same suite-pass profile -> same hash)". (`conformance.py:1` "get the SAME verdict + hash" / `:5` "on this machine, reproducibly" are already correctly scoped — may stay.)
- **Re-derive:** `benchmarks/conformance.py` (`--json` -> conformant:true, result_hash sha256:3d3b5cf39aaffef23fa8562b, byte-identical to committed `outputs/conformance_result.json`); SUITE `:25-34`; hash-over-[(id,pass)] `:50-51`; `/api/gate/verify` route `capas_api.py:100` (`capas.verify_audit_hash`); signed cert `capas_certstore.py:45-57`.

### C3 · `capas_certstore_signature_scope` — REWRITE (verb-scope over-reach)
- **Where:** `capas_certstore.py:4-8` — module docstring
- **CURRENT (lines 5-7):** "The signature binds the certificate CONTENT — it proves the artifact was issued by CAPAS and not altered. It does NOT certify the claim is true (the GIGO ceiling): it certifies that THIS verdict was deterministically produced from THIS evidence by THIS engine version."
- **HIT:** "it certifies that THIS verdict was deterministically produced" credits the HMAC with a guarantee it cannot deliver: an HMAC over content proves only this exact record was signed by this key and unaltered — NOT that production was deterministic (a key-holder could sign a non-deterministically-produced verdict and verify() passes). The determinism guarantee lives in the engine (`capas_verify.py:1255` "decision_path: deterministic; no LLM"), independently re-checkable via the conformance mark — not in the certstore.
- **-> NEW (replace lines 5-7):**
  > The signature binds the certificate CONTENT: it is tamper-evident, non-repudiable proof that this exact record — including the engine version (capas_version) and the full evidence/verdict — was signed by the CAPAS key and not altered since (HMAC-SHA256 over canonical JSON, verified by recomputation). It does NOT certify the claim is true (the GIGO ceiling). It also does not, on its own, prove the verdict was produced deterministically: that guarantee comes from the engine's no-LLM decision path (capas_verify.py: "decision_path": "deterministic; no LLM") and is independently re-checkable by anyone via the self-run conformance mark (python3 benchmarks/conformance.py — the no_llm_verdict suite must PASS). The signature's job is to bind THIS engine-produced record to THIS engine version and make it non-repudiable; the conformance mark + signed certificate together are the auditable artifact, not the ~40 lines of HMAC.
- **Re-derive:** `capas_certstore.py:45-46` (HMAC-SHA256 over _canonical), `:49-58` (issue signs the tuple), `:71-80` (verify recomputes + compare_digest); `capas_verify.py:1255` ("decision_path": "deterministic; no LLM"); `capas_admissibility.py:1-6`; `benchmarks/conformance.py` with `no_llm_verdict` pass:true in `outputs/conformance_result.json`.

### C4 · `claims-registry-self-gating-moat` — REWRITE (scope creep + copyable rung)
- **Where:** `docs/CLAIMS_REGISTRY.md:3-6,25` — kind: governance
- **CURRENT:** "CAPAS gates its own claims. Every public number on this site is itself an admissible claim: CLOSED / BACKED / SCOPED, nothing bare. Ledger: 4 CLOSED · 5 BACKED · 3 SCOPED · 0 bare."
- **HIT:** (1) Scope creep — "every public number on this site" is broader than what the release gate (`generate_proof_ledger.py`) governs (12 enumerated rows); registry §27-29 concedes the capacity/savings widget + 1,238/78.4% grid are UI, NOT gated rows. (2) Copyable rung — a self-status registry + bare-claim release gate is ~150-line CI discipline; the asset is the evidence the rows cite. (The count IS backed — gate ran green, {CLOSED:4, BACKED:5, SCOPED:3} | bare:0.)
- **-> NEW (header block, lines 3-6):**
  > **CAPAS gates its own claims.** Every headline claim in this registry carries a machine-checked admissibility status — `CLOSED` (proven by a passing test), `BACKED` (regenerates from a command, value+hash recorded), or `SCOPED` (a declared estimate stated with its exact corpus and the artifact that would upgrade it). Nothing in this registry is bare; the release gate `python3 benchmarks/generate_proof_ledger.py` subprocess-runs each backing and exits non-zero on any bare or broken claim. Run the command in the *Re-derive* column to reproduce a claim yourself. (`admissible` here means "carries a re-derivation command," NOT "measured/true in production" — see §32, the GIGO ceiling. UI numbers outside this registry are illustrative, not gated; see §27-29.) The copyable part is this self-gating wrapper; the moat is the evidence the rows cite — the self-run conformance mark and the signed certificate (`benchmarks/conformance.py` + `capas_certstore.sign`), the gate calculus, and the audit_hash determinism.
- **-> NEW (ledger line, line 25):**
  > **Ledger:** 4 CLOSED · 5 BACKED · 3 SCOPED · 0 bare (machine-counted; regenerate with `python3 benchmarks/generate_proof_ledger.py`).
  - **Applier note:** any marketing surface lifting line 3 must also carry the §31-32 GIGO disclaimer.
- **Re-derive:** `benchmarks/generate_proof_ledger.py` (ran green, exit 0, {'CLOSED':4,'BACKED':5,'SCOPED':3} | bare:0 | broken:0, RELEASE GATE: PASS); `benchmarks/conformance.py` + `benchmarks/attest_conformance.py`; `capas_certstore.py:sign`; leakage §27-29; GIGO §31-32.

---

## BLOCK D — Quantum ingest-log REWRITE + the one SCOPE fix

### D1 · `motor_ingest_8` — REWRITE (aspirational-as-fact frontier + copyable rung)
- **Where:** `docs/MOTOR_INGEST_LOG.md:26` — row #8 of the 2026-06-24 v1.6.0 ingest table
- **CURRENT:** "commitment_depth predicts the quantum→classical crossover (device-calibrated frontier) [SCOPED·FLAG maturity: same-device validated, cross-device pending]"
- **HIT:** (1) Aspirational-as-fact + unbacked — "predicts… device-calibrated frontier" is present-tense capability, but `verify_quantum_commitment.py:6-7` + `capas_quantum_physics.py:295-297` are explicit it "pins the contract (not the empirical frontier)"; the 5/5 test proves only the `<` defeater. Empirical leg cited to `physics-magnitude-lab` which returns ZERO grep hits for commitment_depth/crossover — CITED, NOT RE-DERIVABLE here. (2) Copyable rung — `gate_quantum_advantage_claim` (`:282-317`) is ~35 lines; the non-copyable asset is the measured frontier this repo can't re-derive. "frontier" overstates a single-device fit. Survives ONLY as bracketed.
- **-> NEW (replace row #8, line 26):**
  > | 8 | `commitment_depth` gate: a quantum-hardness claim past the supplied commitment depth is flagged classically-reproducible | **CLOSED (gate contract) · SCOPED·FLAG (frontier evidence)** | **Gate = CLOSED here:** `benchmarks/verify_quantum_commitment.py` (5/5, exit 0) test-locks the defeater — the `<` rule fires iff a hardness claim's depth exceeds the supplied commitment depth (`capas_quantum_physics.py:282-317`; SCOPE disclaimer `:295-297`). **Frontier = supplied evidence, NOT re-derivable here:** the empirical commitment_depth→crossover mapping is cited to upstream `physics-magnitude-lab` (same-device validated, cross-device pending) and returns zero grep hits in this repo — CAPAS *gates* a supplied same-device commitment depth, it does not *measure* or *predict* the frontier. The defensible asset is not the ~35-line gate (copyable) but CAPAS's self-run conformance mark + signed certificate over the contract (`benchmarks/conformance.py`, `capas_certstore.py`). The "no surveyed tool does this" claim is honest only survey-scoped (see #7). **Upgrade to drop the FLAG:** ingest the upstream same-device artifact into this repo + add a second device. |
  - **Note:** leave Net/Sent-back paragraphs (lines 28-31) as-is; they already tag #8 same-device-only.
- **Re-derive:** `benchmarks/verify_quantum_commitment.py` (5/5, exit 0 — contract only); `capas_quantum_physics.py:282-317` + `:295-297`; `docs/CLAIMS_REGISTRY.md:15`. Frontier/predictive leg: NONE re-derivable (`../physics-magnitude-lab` = 0 grep hits). Moat: `benchmarks/conformance.py` + `capas_certstore.py`.

### D2 · `claim-beat-vendor-benchmark` — **SCOPE** (true, but device-identity must be bounded)
- **Where:** `docs/index.html:568-576` — "Proof on real hardware" (h2 `:568`, sub `:569`, honest-scope `:576`)
- **CURRENT:** "We beat a vendor benchmark with the vendor's own numbers. ... IBM's headline gate-error figure is an optimistic lower bound — for real circuits it under-states by 3–10× (Proctor, Nat. Phys. 2022). From the same published calibration fields, CAPAS re-derives the complete error budget (2–11× the error the headline hid)."
- **HIT:** Survives as truth but conflates two devices: the displayed 1.9e-2 / 11× is `ibm_fez` q9-q10 (`BEATING_THE_BENCHMARK.md:26-37`), but the live-validation prose reads as `ibm_kingston` Q121 (`:589`) — no artifact runs `complete_error_budget` on Q121 to reproduce 1.9e-2. Stale provenance: the "2.53× / XEB" leg cites `/tmp/xeb_results.json` (`BEATING_THE_BENCHMARK.md:4`) — NOT in git ls-files, not re-derivable.
- **-> NEW (body :569 + honest-scope :576; the three stat tiles :571-574 stay as-is):**
  > We beat a vendor benchmark with the vendor's own numbers. IBM's headline gate-error figure is an optimistic lower bound — for structured circuits it under-states by 3–10× (Proctor, Nat. Phys. 2022). From the same published calibration fields, CAPAS re-derives the complete error budget — fully auditable, no hardware required.
  >
  > [stat tiles unchanged: IBM headline (RB) 1.6×10⁻³ → Re-derivable complete floor 1.9×10⁻² → Error the headline hid 2–11×]
  >
  > Honest scope: the gap is ~2× on a healthy qubit and reaches 11× on a dephasing-limited one. The 1.9×10⁻² worst case shown is ibm_fez q9–q10, re-derived term-by-term from the vendor's published fields by complete_error_budget (each term marked EXACT vs. literature-estimate; the 3–10× structured-circuit band is a cited Proctor range, not our finding). The live leg is a separate device: on ibm_kingston, CAPAS re-found the chip's one anomalous qubit (Q121) and its bad-coupler cluster from calibration alone, and admitted a real Bell measurement (measured 0.045 vs. re-derived floor 0.020 ≈ 2.2×, ADMISSIBLE under two independent oracles). The arithmetic is textbook and copyable; what is not is the fail-closed conformance discipline that refuses the optimistic headline as admissible — the verdict any third party can re-run and challenge. Full method →
  - **Companion fix (separate file):** `docs/BEATING_THE_BENCHMARK.md:4` — commit the XEB results under `benchmarks/` OR remove the `/tmp/xeb_results.json` citation and the 2.53× leg; do not surface 2.53× on the page until committed (or down-scope to the Bell JSON, 2.2×).
- **Re-derive:** `capas_quantum_physics.complete_error_budget` (`capas_quantum_physics.py:468-516`: exact_floor `:493`, exact_gap_x `:505`, structured_band [3,10] `:501`, provenance `:510-513`) on `ibm_fez` q9-q10 = 1.86e-2 (11.4×), per `docs/BEATING_THE_BENCHMARK.md:26-37`; 2.2× live leg = `benchmarks/kingston_real_bell_verdict.json` (measured 0.0449 vs floor 0.020453, ADMISSIBLE, two oracles); `docs/CLAIMS_REGISTRY.md:19` (BACKED). XEB 2.53× leg NOT re-derivable (`/tmp/xeb_results.json` not committed).

---

## Apply order summary

1. **A1–A4** — strip unbacked universal-negatives / comparative-negatives (highest legal/credibility risk).
2. **B1–B4** — demote aspirational "the standard / the moat" to mechanism + honest-scope. (B1 + B2 are the same `index.html` "Why it compounds" section — apply as one merged rewrite of lines 600-606.)
3. **C1–C4** — fix GOVERNANCE / certstore / registry over-claims (escrow not executed, HMAC scope, registry scope creep).
4. **D1** — re-word quantum ingest row #8; **D2** — SCOPE the vendor-benchmark device conflation + stale `/tmp` XEB provenance.

**File discipline:** every target above is a curated live file — apply by hand from this queue.
This queue is the only new file written.
